import SectionWrapper from "./SectionWrapper";

const Workout = (props) => {
  const { workout } = props;
  return (
    <SectionWrapper
      header={"Welcome to"}
      title={["The", "Danger", "Zone"]}
    >
      {" "}
    </SectionWrapper>
  );
};

export default Workout;
